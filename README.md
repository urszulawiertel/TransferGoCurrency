# CurrencyConverter

Initial SwiftUI iOS app structure with a reusable `CurrencyConverterFeature` module.

The app target is intentionally minimal and only hosts `CurrencyConverterView`.
Business logic is not implemented yet.

